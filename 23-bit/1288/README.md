# 1288. 새로운 불면증 치료법

## 문제 출처
[SWEA](https://swexpertacademy.com/main/code/problem/problemDetail.do?contestProbId=AV18_yw6I9MCFAZN)

## 💡 접근 방식

### 1. 사용 알고리즘
* **비트마스킹(Bitmasking)**
* **시뮬레이션(Simulation)** / **브루트포스(Brute Force)**

### 2. 문제 풀이 과정
1.  0부터 9까지의 숫자가 모두 등장했는지 여부를 효율적으로 확인하기 위해 **비트마스킹**을 사용합니다. 10비트 크기의 정수(`numbers`)를 0으로 초기화합니다.
2.  양을 세는 횟수 `count`를 1로 초기화하고 `while` 루프를 시작합니다.
3.  현재 양의 번호(`current_number`)를 `N * count`로 계산합니다.
4.  `current_number`가 0보다 큰 동안, `% 10` (나머지) 연산으로 마지막 자릿수 `digit`를 추출하고 `// 10` (몫) 연산으로 `current_number`를 갱신하며 각 자릿수를 분리합니다.
5.  추출한 `digit`에 대해 `numbers |= (1 << digit)` 연산을 수행하여, `numbers`의 `digit`번째 비트를 1로 세팅(방문 처리)합니다.
6.  `current_number`의 모든 자릿수를 처리한 후, `numbers`가 0부터 9까지 모든 비트가 켜진 상태(`(1 << 10) - 1`)인지 확인합니다.
7.  모든 비트가 켜졌다면(모든 숫자를 봤다면), `while` 루프를 `break`합니다.
8.  그렇지 않다면, `count`를 1 증가시키고 다음 턴을 진행합니다.
9.  루프가 종료된 시점의 `N * count` (즉, 마지막으로 확인한 `current_number`)가 정답이 됩니다.

### 💻 코드
* [1288.py](1288.py)
